{
  "name": "appointment-slot-optimizer",
  "version": "0.1.0",
  "description": "Constraint-based scheduling engine for healthcare clinics",
  "transport": "stdio",
  "tools": [
    {
      "name": "find_available_slots",
      "description": "Find scored available appointment slots for a date and type",
      "endpoint": "GET /api/slots"
    },
    {
      "name": "create_booking",
      "description": "Create an idempotent booking with conflict prevention",
      "endpoint": "POST /api/bookings"
    },
    {
      "name": "list_bookings",
      "description": "List bookings with date, provider, and status filters",
      "endpoint": "GET /api/bookings"
    },
    {
      "name": "get_booking",
      "description": "Get a single booking by ID",
      "endpoint": "GET /api/bookings/{id}"
    },
    {
      "name": "cancel_booking",
      "description": "Cancel a booking and get backfill candidates",
      "endpoint": "PUT /api/bookings/{id}/cancel"
    },
    {
      "name": "get_schedule",
      "description": "Get schedule view with utilization per provider and date range",
      "endpoint": "GET /api/schedule"
    },
    {
      "name": "get_stats",
      "description": "Get daily statistics: utilization, booking count, no-shows",
      "endpoint": "GET /api/stats"
    },
    {
      "name": "health_check",
      "description": "Check API health status",
      "endpoint": "GET /api/health"
    }
  ]
}
